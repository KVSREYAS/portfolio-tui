# Backend URL (no trailing slash). Required for production when API is on another host.
# Leave empty for `npm run dev` — Vite proxies /api to http://127.0.0.1:5000.
VITE_API_URL=https://your-backend.up.railway.app
